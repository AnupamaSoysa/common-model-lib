# common-model

✅ Standalone Maven library containing shared **models / DTOs**.

## Build
```bash
mvn -B clean install
```

## Maven coordinates
- groupId: `com.anupamasoysa`
- artifactId: `common-model`
- version: `0.0.1-SNAPSHOT`
